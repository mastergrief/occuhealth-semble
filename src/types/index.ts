/**
 * Type Definitions Index
 *
 * Re-exports all shared types for convenient imports.
 *
 * @example
 * ```tsx
 * import { DoctorContextType, FitForWorkStatus } from "@/types";
 * ```
 */

// Doctor Portal Types
export type {
  DoctorContextType,
  FitForWorkStatus,
  ReportFormData,
  AppointmentStatus,
  SlotStatus,
  TimeSlot,
  DashboardStats,
} from "./doctor";
